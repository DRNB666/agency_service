<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.turing.code.tenant.mapper.TenantOrderBillsMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.turing.code.tenant.pojo.TenantOrderBills" autoMapping="true">
        <id column="id" property="id" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, status, remark, create_time, update_time, test_time, version, order_id, amount, receipt_time, registration_id, audit_id, audit_time, rejected_reason
    </sql>
    <select id="confirmList" resultType="com.turing.code.tenant.pojo.vo.TenantBillsConfirmVo">
        SELECT
        uo.total_amount AS orderAmount,
        uo.count AS `count`,
        ui.NAME AS userName,
        tob.*
        FROM
        tenant_order_bills tob
        LEFT JOIN user_order uo ON tob.order_id = uo.order_id
        LEFT JOIN user_info ui ON uo.us_id = ui.id
        WHERE
        uo.tenant_id = #{param.pId}
        <if test="param.phone !=null and param.phone.toString() !=''">
             AND ui.mobile=#{param.phone}
        </if>
        <if test="param.orderId !=null and param.orderId.toString() !=''">
             AND uo.order_id=#{param.orderId}
        </if>
        ORDER BY tob.create_time DESC
    </select>

</mapper>
