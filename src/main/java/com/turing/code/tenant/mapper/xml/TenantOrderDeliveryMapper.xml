<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.turing.code.tenant.mapper.TenantOrderDeliveryMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.turing.code.tenant.pojo.TenantOrderDelivery" autoMapping="true">
        <id column="id" property="id"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, status, remark, create_time, update_time, test_time, version, order_pay_id, order_id, count, operator_id, logistics_company, logistics_number
    </sql>
    <select id="devList" resultType="com.turing.code.tenant.pojo.vo.TenantOrderDevVo">
        select
        tod.*,ti.name as tenantName
        from
        tenant_order_delivery tod left join tenant_info ti on ti.id=tod.operator_id
        where
        order_id = #{orderId}
    </select>

</mapper>
